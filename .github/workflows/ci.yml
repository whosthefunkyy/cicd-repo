name: CICDPro

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:   
      - uses: actions/checkout@v4
        name: Run script
      - name: Say hello
        run: echo "Hello"
          
      - name: Open check
        run: |
           ls -la
           pwd
